version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=cms_user
      - POSTGRES_PASSWORD=cms_pass
      - POSTGRES_DB=cms_db
    ports:
      - "5432:5432"

volumes:
  postgres_data:

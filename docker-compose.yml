# ─── S4Carlisle CMS — Docker Compose ─────────────────────────────────────────
#
# Prerequisites before first `docker compose up`:
#   1. cp .env.example .env  && edit .env with real values
#   2. cp ai_structuring_backend/.env.example ai_structuring_backend/.env  && edit
#   3. Build the React frontend:
#        cd frontend && npm ci && npm run build && cd ..
#   4. Create host directories expected by volume mounts:
#        mkdir -p /opt/cms_runtime/data/uploads outputs data temp_reports
#
# Start all services:
#   docker compose up -d
#
# View logs:
#   docker compose logs -f backend
#
# Run database migrations (first time or after updates):
#   docker compose exec backend alembic upgrade head

services:

  # ── PostgreSQL ────────────────────────────────────────────────────────────
  db:
    image: postgres:15
    container_name: cms_db
    ports:
      # Bound to 127.0.0.1 to prevent external network access.
      # Mapped to 5433 to avoid collision with any local PostgreSQL on port 5432.
      # For local dev, use: DATABASE_URL=postgresql://cms_user:cms_password@127.0.0.1:5433/cms_db
      - "127.0.0.1:5433:5432"
    env_file: .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER:             ${POSTGRES_USER:-cms_user}
      POSTGRES_PASSWORD:         ${POSTGRES_PASSWORD:-cms_password}
      POSTGRES_DB:               ${POSTGRES_DB:-cms_db}
      POSTGRES_HOST_AUTH_METHOD: md5
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cms_user} -d ${POSTGRES_DB:-cms_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

  # ── Redis ─────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: cms_redis
    ports:
      # Bound to 127.0.0.1 — Redis has no auth by default, never expose externally.
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

  # ── FastAPI Backend ───────────────────────────────────────────────────────
  backend:
    image: ${BACKEND_IMAGE:-cms_backend}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cms_backend
    env_file: .env
    volumes:
      # Use named volumes for uploads and runtime data on Windows
      - cms_runtime_data:/opt/cms_runtime/data
      - ./outputs:/app/outputs
      - ./data:/app/data
      - ./temp_reports:/app/temp_reports
      # ref_cache.json is maintained inside the named volume to avoid the
      # "directory created instead of file" pitfall of bind-mounting a file
      # that doesn't exist yet.
      - ref_cache:/opt/cms_runtime
      # Live app code — overrides baked-in image files so changes are picked up on restart
      - ./app:/app/app
      - ./frontend/src/config/rbac_config.json:/app/app/domains/auth/rbac_config.json
    environment:
      REDIS_URL: redis://redis:6379/0
      COOKIE_SECURE: "false"
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ── Celery Worker ─────────────────────────────────────────────────────────
  celery_worker:
    image: ${BACKEND_IMAGE:-cms_backend}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cms_celery_worker
    env_file: .env
    volumes:
      - cms_runtime_data:/opt/cms_runtime/data
      - ./outputs:/app/outputs
      - ./data:/app/data
      - ./temp_reports:/app/temp_reports
      - ref_cache:/opt/cms_runtime
      - ./app:/app/app
      - ./frontend/src/config/rbac_config.json:/app/app/domains/auth/rbac_config.json
    environment:
      REDIS_URL: redis://redis:6379/0
      RUN_MIGRATIONS: "false"
    command: celery -A app.core.celery_app worker --loglevel=info --concurrency=4 -Q main-queue
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      disable: true
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ── Collabora Online (LibreOffice) ────────────────────────────────────────
  # HOST_DOMAIN and HOST_PORT are read from .env.
  # They must match the public address browsers use to reach this server,
  # because Collabora embeds that address in editor iframe URLs.
  collabora:
    image: collabora/code
    container_name: cms_collabora
    env_file: .env
    environment:
      # Allow WOPI requests originating from both public URL (for browser)
      # and internal Docker service name (for Collabora->backend communication).
      aliasgroup1: "http://${HOST_DOMAIN:-localhost}:${HOST_PORT:-8080}"
      aliasgroup2: "http://backend:8000"
      DONT_GEN_SSL_CERT: "YES"
      extra_params: >-
        --o:ssl.enable=false
        --o:ssl.termination=false
        --o:net.proto=IPv4
        --o:server_name=${HOST_DOMAIN:-localhost}:${HOST_PORT:-8080}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "9980:9980"
    restart: always

  onlyoffice:
    image: onlyoffice/documentserver
    container_name: cms_onlyoffice
    ports:
      - "8083:80"
    # OnlyOffice uses shared memory heavily for document co-authoring.
    shm_size: '2gb'
    environment:
      JWT_ENABLED: "false"
      JWT_SECRET: ${ONLYOFFICE_JWT_SECRET:-secret}
      JWT_HEADER: "Authorization"
      JWT_IN_BODY: "true"
      ALLOW_PRIVATE_IP_ADDRESS: "true"
    restart: always

  # ── AI Structuring Service ────────────────────────────────────────────────
  ai_structuring:
    image: ${AI_IMAGE:-cms_ai_structuring}
    build:
      context: ./ai_structuring_backend
      dockerfile: Dockerfile
    container_name: cms_ai_structuring
    env_file: ./ai_structuring_backend/.env
    expose:
      - "5000"
    volumes:
      - ai_structuring_uploads:/app/uploads
      - ai_structuring_outputs:/app/outputs
      - ai_structuring_data:/app/data
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: always

  # ── AI Structuring Celery Worker ──────────────────────────────────────────
  ai_structuring_worker:
    image: ${AI_IMAGE:-cms_ai_structuring}
    build:
      context: ./ai_structuring_backend
      dockerfile: Dockerfile
    container_name: cms_ai_structuring_worker
    command: celery -A celery_worker worker --loglevel=info --concurrency=2
    stop_grace_period: 600s
    env_file: ./ai_structuring_backend/.env
    volumes:
      - ai_structuring_uploads:/app/uploads
      - ai_structuring_outputs:/app/outputs
      - ai_structuring_data:/app/data
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
      ai_structuring:
        condition: service_healthy
    healthcheck:
      disable: true
    restart: always

  # ── Nginx Reverse Proxy ───────────────────────────────────────────────────
  # Serves:
  #   /ui/           → React SPA (built frontend/dist mounted read-only)
  #   /api/          → FastAPI backend
  #   /wopi/         → FastAPI WOPI endpoints (Collabora callbacks)
  #   /hosting/      → Collabora static assets
  #   /browser/      → Collabora static assets
  #   /cool/ /coolws/→ Collabora editor + WebSocket
  #   /              → FastAPI legacy SSR pages
  #
  # The React frontend must be built BEFORE starting this service:
  #   cd frontend && npm ci && npm run build
  nginx:
    image: nginx:alpine
    container_name: cms_nginx
    ports:
      - "${HOST_PORT:-8080}:80"
      - "8443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend/dist:/var/www/ui:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - certbot_webroot:/var/www/certbot:ro
    depends_on:
      backend:
        condition: service_healthy
      onlyoffice:
        condition: service_started
      collabora:
        condition: service_started
    restart: always

volumes:
  postgres_data:
  certbot_webroot:
  ref_cache:
  cms_runtime_data:
  ai_structuring_uploads:
  ai_structuring_outputs:
  ai_structuring_data:
